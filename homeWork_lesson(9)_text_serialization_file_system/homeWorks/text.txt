прочитать employees из JSON

спросить у пользователя год
проверить, что это число

создать пустой список heights

для каждого employee:
    достать год из employee["birthday"]
    превратить его в int

    если birth_year < введённый год:
        добавить employee["height"] в heights

если heights пустой:
    вывести, что подходящих сотрудников нет
иначе:
    average = sum(heights) / len(heights)
    вывести average