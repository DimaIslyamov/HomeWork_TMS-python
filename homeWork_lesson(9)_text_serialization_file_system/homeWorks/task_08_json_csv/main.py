import os
import json
import pandas as pd

current_directory = os.getcwd()

employees_json_path = os.path.join(current_directory, 'employees.json')
employees_csv_path = os.path.join(current_directory, 'employees.csv')
